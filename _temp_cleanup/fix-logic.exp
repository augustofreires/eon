#!/usr/bin/expect -f

set timeout 30

spawn ssh -o StrictHostKeyChecking=no -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect "*assword*"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"
expect "*#*"

send "cd /root/eon\r"
expect "*#*"

puts "🔧 CORRIGINDO A LÓGICA DO RETURN..."

# Corrigir para retornar response.data.bots || response.data em vez de sempre response.data
send "sed -i '102s/? response\\.data : \\[\\]/? (response.data.bots || response.data) : []/' ./client/src/pages/OperationsPage.tsx\r"
expect "*#*"

# Verificar
send "sed -n '102p' ./client/src/pages/OperationsPage.tsx\r"
expect "*#*"

puts "🏗️ BUILD FINAL..."
send "cd client && npm run build\r"
expect {
    "*build folder is ready*" {
        puts "✅ BUILD OK!"
    }
    "*compiled successfully*" {
        puts "✅ BUILD OK!"
    }
    timeout {
        puts "⏰ Building..."
    }
}
expect "*#*"

send "cd /root/eon && pm2 restart all\r"
expect "*#*"

puts ""
puts "🎯 LÓGICA CORRIGIDA!"
puts "🧪 AGORA DEVE FUNCIONAR: https://iaeon.site/operations"

interact