#!/usr/bin/expect -f

set timeout 60

spawn ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"

expect "root@"

send "cd /root/eon\r"
expect "root@"

# Kill all node processes
send "pkill -9 -f node\r"
expect "root@"

send "sleep 3\r"
expect "root@"

# Kill anything on port 3001
send "lsof -ti:3001 | xargs kill -9 2>/dev/null || echo 'Port free'\r"
expect "root@"

send "sleep 2\r"
expect "root@"

# Start server
send "nohup node server/index.js > server_final.log 2>&1 &\r"
expect "root@"

send "sleep 5\r"
expect "root@"

# Test
send "curl -s http://localhost:3001/api/health\r"
expect "root@"

puts ""
puts "✅ SERVIDOR REINICIADO!"
puts ""
puts "🌐 TESTE: https://iaeon.site"
puts "📋 LOGIN: admin@iaeon.com / admin123"

send "exit\r"
expect eof