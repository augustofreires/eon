#!/usr/bin/expect -f

set timeout 60
set password "62uDLW4RJ9ae28EPVfp5yzT##"

spawn ssh root@31.97.28.231

expect {
    "passphrase" {
        send "\r"
        exp_continue
    }
    "password:" {
        send "$password\r"
        exp_continue
    }
    "# " {
        puts "Connected to test switch account"
    }
}

send "cd /root/eon\r"
expect "# "

puts "Testing switch to virtual account..."
send "curl -X POST http://localhost:3001/api/auth/deriv/switch-account -H \"Content-Type: application/json\" -d '{\"is_virtual\": true, \"loginid\": \"VRTC9858183\"}' -H \"Authorization: Bearer fake-token-for-test\"\r"
expect "# "

puts "Waiting for PM2 logs to update..."
send "sleep 2\r"
expect "# "

puts "Checking recent logs..."
send "pm2 logs iaeon-server --lines 20 | grep -E \"switch|saldo|balance\" | tail -10\r"
expect "# "

interact
