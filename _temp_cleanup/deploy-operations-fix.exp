#!/usr/bin/expect -f

set timeout 60
set password "62uDLW4RJ9ae28EPVfp5yzT##"

spawn ssh -o StrictHostKeyChecking=no -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect {
    "*password:" {
        send "$password\r"
        exp_continue
    }
    "*# " {
        puts "\n🚀 DEPLOY CORREÇÃO PAINEL DE OPERAÇÕES..."

        # Copiar novo build
        send "cd /root/eon/client && cp -r build/* /var/www/html/\r"
        expect "*# "

        # Verificar novo arquivo
        send "ls -la /var/www/html/static/js/main*.js\r"
        expect "*# "

        # Reiniciar nginx
        send "systemctl reload nginx\r"
        expect "*# "

        puts "\n✅ DEPLOY CONCLUÍDO COM CORREÇÃO DO PAINEL!"
        puts "🌐 Teste: https://iaeon.site/operations"
        puts ""
        puts "🎯 CORREÇÕES APLICADAS:"
        puts "   ✓ useEffect adicionado para carregar bots quando Deriv conectar"
        puts "   ✓ Painel de operações agora deve aparecer após login Deriv"
        puts "   ✓ Saldo da conta e bots disponíveis devem ser visíveis"
        puts ""
        puts "🔑 CREDENCIAIS PARA TESTE:"
        puts "   Cliente: cliente@iaeon.com / 123456"
        puts "   Admin: admin@iaeon.com / 123456"

        send "exit\r"
    }
}

expect eof