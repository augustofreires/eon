#!/usr/bin/expect -f

set timeout 60

# Upload fixes
puts "🔧 Uploading OAuth fixes..."

spawn scp -o PreferredAuthentications=password -o PubkeyAuthentication=no oauth-final-fixes.tar.gz root@31.97.28.231:/tmp/

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"

expect eof

puts "✅ Upload complete. Deploying fixes..."

# Deploy fixes
spawn ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"

expect "root@"

send "cd /root/eon\r"
expect "root@"

send "pkill -f node\r"
expect "root@"

send "sleep 2\r"
expect "root@"

# Extract fixes
send "tar -xzf /tmp/oauth-final-fixes.tar.gz\r"
expect "root@"

send "echo '✅ OAuth fixes aplicadas'\r"
expect "root@"

# Build
send "cd client && npm run build\r"
expect -timeout 120 "root@"

# Start server
send "cd /root/eon && nohup node server/index.js > oauth_fixes.log 2>&1 &\r"
expect "root@"

send "sleep 3\r"
expect "root@"

# Test
send "curl -s http://localhost:3001/api/health\r"
expect "root@"

puts ""
puts "✅ OAUTH FIXES APLICADAS COM SUCESSO!"
puts ""
puts "🔧 CORREÇÕES:"
puts "• Popup fecha automaticamente em 500ms"
puts "• DerivAccountPanel usa deriv_connected"
puts "• Token salvo para compatibilidade"
puts "• Logs melhorados para debug"
puts ""
puts "🌐 TESTE: https://iaeon.site"
puts "📋 LOGIN: admin@iaeon.com / admin123"
puts ""
puts "🚀 Teste novamente o OAuth - popup deve fechar e saldo aparecer!"

send "exit\r"
expect eof