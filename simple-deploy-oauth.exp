#!/usr/bin/expect -f

set timeout 60

spawn ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"

expect "root@"

send "cd /root/eon\r"
expect "root@"

# Stop server
send "pkill -f node\r"
expect "root@"

send "sleep 2\r"
expect "root@"

# Rebuild frontend
send "echo '=== BUILDING FRONTEND ==='\r"
expect "root@"

send "cd client && npm run build\r"
expect -timeout 120 "root@"

# Start server
send "cd /root/eon\r"
expect "root@"

send "nohup node server/index.js > oauth_test.log 2>&1 &\r"
expect "root@"

send "sleep 3\r"
expect "root@"

# Test server
send "curl -s http://localhost:3001/api/health || echo 'Server not ready'\r"
expect "root@"

puts ""
puts "✅ DEPLOY CONCLUÍDO!"
puts ""
puts "🌐 Teste em: https://iaeon.site"
puts "📋 Login: admin@iaeon.com / admin123"
puts ""
puts "Agora teste o OAuth Deriv no painel!"

send "exit\r"
expect eof