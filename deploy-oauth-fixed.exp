#!/usr/bin/expect -f

set timeout 60

# Upload fixed tar
puts "📦 Uploading fixed OAuth optimizations..."

spawn scp -o PreferredAuthentications=password -o PubkeyAuthentication=no oauth-optimizations-fixed.tar.gz root@31.97.28.231:/tmp/

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"

expect eof

puts "✅ Upload complete. Deploying..."

# Deploy
spawn ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"

expect "root@"

send "cd /root/eon\r"
expect "root@"

send "pkill -f node\r"
expect "root@"

send "sleep 2\r"
expect "root@"

# Extract fixed files
send "tar -xzf /tmp/oauth-optimizations-fixed.tar.gz\r"
expect "root@"

# Build
send "cd client && npm run build\r"
expect -timeout 120 "root@"

# Start server
send "cd /root/eon && nohup node server/index.js > oauth_final.log 2>&1 &\r"
expect "root@"

send "sleep 3\r"
expect "root@"

# Test
send "curl -s http://localhost:3001/api/health\r"
expect "root@"

puts ""
puts "✅ DEPLOY OAUTH OTIMIZADO CONCLUÍDO!"
puts ""
puts "🔧 AuthContext otimizado com:"
puts "• Cache de 30 segundos"
puts "• Prevenção de múltiplas execuções"
puts "• Loading states melhorados"
puts ""
puts "🌐 TESTE: https://iaeon.site"
puts "📋 LOGIN: admin@iaeon.com / admin123"
puts ""
puts "🚀 OAuth Deriv pronto para teste!"

send "exit\r"
expect eof