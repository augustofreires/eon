#!/usr/bin/expect -f
set timeout 30

spawn ssh -o StrictHostKeyChecking=no -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231
expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"
expect "#"

send "cd /root/eon\r"
expect "#"

# Testar endpoint sem token (deve dar 401/403)
send "curl -I https://iaeon.site/api/bots\r"
expect "#"

# Verificar logs para ver se a requisição chegou
send "pm2 logs iaeon-server --lines 5\r"
expect "#"

send "exit\r"
expect eof
