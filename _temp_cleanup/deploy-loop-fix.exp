#!/usr/bin/expect -f

set timeout 60
set password "62uDLW4RJ9ae28EPVfp5yzT##"

# Upload build completo
spawn scp -o StrictHostKeyChecking=no -o PreferredAuthentications=password "/Users/augustofreires/Desktop/Bots deriv/client/build/index.html" "/Users/augustofreires/Desktop/Bots deriv/client/build/static/js/main.939f0c07.js" root@31.97.28.231:/tmp/

expect {
    "*password:" {
        send "$password\r"
        expect eof
    }
}

# Deploy
spawn ssh -o StrictHostKeyChecking=no -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect {
    "*password:" {
        send "$password\r"
        exp_continue
    }
    "*# " {
        puts "\n🔧 DEPLOY CORREÇÃO LOOP INFINITO..."

        # Copiar index.html atualizado
        send "cp /tmp/index.html /var/www/html/\r"
        expect "*# "

        # Copiar novo JS
        send "cp /tmp/main.939f0c07.js /var/www/html/static/js/\r"
        expect "*# "

        # Verificar arquivo correto no index
        send "grep 'main\\.' /var/www/html/index.html\r"
        expect "*# "

        # Verificar arquivo existe
        send "ls -la /var/www/html/static/js/main.939f0c07.js\r"
        expect "*# "

        # Reload nginx
        send "systemctl reload nginx\r"
        expect "*# "

        puts "\n✅ DEPLOY CORREÇÃO LOOP CONCLUÍDO!"
        puts "🌐 TESTE: https://iaeon.site/operations"
        puts ""
        puts "🎯 CORREÇÕES APLICADAS:"
        puts "   ✓ useCallback sem dependências circulares"
        puts "   ✓ Flag botsLoadedRef apenas após sucesso"
        puts "   ✓ Reset da flag quando bots são limpos"
        puts "   ✓ useEffect sem função nas dependências"
        puts "   ✓ Debug global: window.debugBotState()"
        puts ""
        puts "🔥 BOTÃO DE RELOAD NÃO DEVE MAIS PISCAR INFINITAMENTE!"

        send "exit\r"
    }
}

expect eof