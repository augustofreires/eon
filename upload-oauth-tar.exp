#!/usr/bin/expect -f

set timeout 60

# First, upload the tar file
puts "📦 Uploading OAuth optimizations..."

spawn scp -o PreferredAuthentications=password -o PubkeyAuthentication=no oauth-optimizations.tar.gz root@31.97.28.231:/tmp/

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"

expect eof

puts "✅ Upload complete. Now deploying..."

# Now connect and deploy
spawn ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"

expect "root@"

send "cd /root/eon\r"
expect "root@"

# Stop server
send "pkill -f node\r"
expect "root@"

send "sleep 2\r"
expect "root@"

# Extract optimizations
send "cd /root/eon && tar -xzf /tmp/oauth-optimizations.tar.gz\r"
expect "root@"

send "echo '✅ Arquivos otimizados aplicados'\r"
expect "root@"

# Build frontend
send "cd client && npm run build\r"
expect -timeout 120 "root@"

# Start server
send "cd /root/eon && nohup node server/index.js > oauth_deploy.log 2>&1 &\r"
expect "root@"

send "sleep 3\r"
expect "root@"

# Test
send "curl -s http://localhost:3001/api/health || echo 'Server starting...'\r"
expect "root@"

puts ""
puts "✅ DEPLOY OAUTH OTIMIZADO CONCLUÍDO!"
puts ""
puts "🔧 MELHORIAS:"
puts "• Cache de 30s nas contas Deriv"
puts "• Prevenção de múltiplas execuções"
puts "• Loading states otimizados"
puts "• UX melhorada para troca de contas"
puts ""
puts "🌐 TESTE: https://iaeon.site"
puts "📋 LOGIN: admin@iaeon.com / admin123"
puts ""
puts "🚀 Agora teste o OAuth Deriv no painel de operações!"

send "exit\r"
expect eof