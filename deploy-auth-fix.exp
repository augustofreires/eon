#!/usr/bin/expect -f

set timeout 120

spawn ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"

expect "root@"

send "cd /root/eon\r"
expect "root@"

# Build frontend
send "echo '=== FAZENDO BUILD DO FRONTEND ==='\\r"
expect "root@"

send "cd client && npm run build\r"
expect "root@"

# Copiar para nginx
send "echo '=== COPIANDO BUILD PARA NGINX ==='\\r"
expect "root@"

send "cp -r build/* /var/www/html/\r"
expect "root@"

# Restart server
send "echo '=== REINICIANDO SERVIDOR ==='\\r"
expect "root@"

send "cd /root/eon\r"
expect "root@"

send "pkill -9 -f node\r"
expect "root@"

send "sleep 3\r"
expect "root@"

send "nohup node server/index.js > server_final.log 2>&1 &\r"
expect "root@"

send "sleep 5\r"
expect "root@"

# Test server
send "curl -s http://localhost:3001/api/health\r"
expect "root@"

puts ""
puts "✅ DEPLOY CONCLUÍDO COM CORREÇÃO JWT!"
puts ""
puts "🌐 TESTE: https://iaeon.site"
puts "📋 LOGIN: admin@iaeon.com / admin123"
puts "🔑 Agora as requisições incluem JWT tokens automaticamente"

send "exit\r"
expect eof