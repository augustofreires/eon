#!/usr/bin/expect -f

set timeout 60

spawn ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"

expect "root@"

send "cd /root/eon\r"
expect "root@"

send "pkill -f node\r"
expect "root@"

send "sleep 2\r"
expect "root@"

# Apenas fazer build com o que já existe e iniciar servidor
send "cd client && npm run build\r"
expect -timeout 120 "root@"

send "cd /root/eon && nohup node server/index.js > oauth_test.log 2>&1 &\r"
expect "root@"

send "sleep 3\r"
expect "root@"

send "curl -s http://localhost:3001/api/health\r"
expect "root@"

puts ""
puts "✅ SERVIDOR INICIADO COM SUCESSO!"
puts ""
puts "🌐 TESTE: https://iaeon.site"
puts "📋 LOGIN: admin@iaeon.com / admin123"
puts ""
puts "Agora teste o OAuth Deriv!"

send "exit\r"
expect eof