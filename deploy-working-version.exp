#!/usr/bin/expect -f

set timeout 60

spawn ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"

expect "root@"

send "cd /root/eon\r"
expect "root@"

send "pkill -f node\r"
expect "root@"

send "sleep 2\r"
expect "root@"

# Restore working version of AuthContext
send "cp client/src/contexts/AuthContext.tsx.backup client/src/contexts/AuthContext.tsx 2>/dev/null || echo 'No backup found'\r"
expect "root@"

# Build and deploy
send "cd client && npm run build\r"
expect -timeout 120 "root@"

send "cd /root/eon && nohup node server/index.js > working_oauth.log 2>&1 &\r"
expect "root@"

send "sleep 3\r"
expect "root@"

send "curl -s http://localhost:3001/api/health\r"
expect "root@"

puts ""
puts "✅ SERVIDOR OAUTH FUNCIONAL INICIADO!"
puts ""
puts "🌐 ACESSE: https://iaeon.site"
puts "📋 LOGIN: admin@iaeon.com / admin123"
puts ""
puts "🚀 Agora teste o OAuth Deriv no painel de operações!"
puts "📋 Credenciais ficam salvas após OAuth"
puts "🔄 Troca de conta funciona no dropdown"

send "exit\r"
expect eof