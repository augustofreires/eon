#!/usr/bin/expect -f

set timeout 30

spawn ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null root@31.97.28.231

expect {
    "Enter passphrase for key" {
        send "\r"
        expect {
            "password:" {
                send "62uDLW4RJ9ae28EPVfp5yzT##\r"
            }
            "Enter passphrase for key" {
                send "\r"
                expect "password:"
                send "62uDLW4RJ9ae28EPVfp5yzT##\r"
            }
        }
    }
    "password:" {
        send "62uDLW4RJ9ae28EPVfp5yzT##\r"
    }
}

expect "# "

# Test client login via frontend URL
send "echo '=== Testing CLIENT login via frontend ==='\r"
expect "# "
send "curl -X POST https://iaeon.site/api/auth/login -H 'Content-Type: application/json' -d '{\"email\":\"cliente@iaeon.com\",\"password\":\"123456\",\"isAdmin\":false}'\r"
expect "# "

# Test admin login via frontend URL
send "echo '=== Testing ADMIN login via frontend ==='\r"
expect "# "
send "curl -X POST https://iaeon.site/api/auth/login -H 'Content-Type: application/json' -d '{\"email\":\"admin@iaeon.com\",\"password\":\"123456\",\"isAdmin\":true}'\r"
expect "# "

# Check what users exist in the database
send "echo '=== Checking users in database ==='\r"
expect "# "
send "psql -d eon_platform -c \"SELECT id, email, name, role, status FROM users;\"\r"
expect "# "

send "exit\r"
expect eof