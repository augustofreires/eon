#!/usr/bin/expect -f

set timeout 60

spawn ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"

expect "root@"

send "cd /root/eon\r"
expect "root@"

# Check server logs for 400 errors
send "echo '=== LOGS DO SERVIDOR (ÚLTIMOS 50 ERROS) ==='\r"
expect "root@"

send "tail -50 server_final.log\r"
expect "root@"

# Test the API with proper authentication to see the exact error
send "echo '=== TESTANDO API COM AUTENTICAÇÃO ==='\r"
expect "root@"

# Get a valid token first
send "TOKEN=$(curl -s -X POST http://localhost:3001/api/auth/login -H 'Content-Type: application/json' -d '{\"email\":\"admin@iaeon.com\",\"password\":\"admin123\"}' | grep -o '\"token\":\"[^\"]*' | cut -d'\"' -f4)\r"
expect "root@"

send "echo 'Token: '$TOKEN\r"
expect "root@"

# Test account-info with authentication
send "echo '--- Testando /api/auth/deriv/account-info ---'\r"
expect "root@"

send "curl -v -H \"Authorization: Bearer \$TOKEN\" http://localhost:3001/api/auth/deriv/account-info 2>&1 | head -20\r"
expect "root@"

# Check if user has deriv data
send "echo '=== VERIFICANDO DADOS DERIV DO USUÁRIO ==='\r"
expect "root@"

send "curl -s -H \"Authorization: Bearer \$TOKEN\" http://localhost:3001/api/auth/deriv/debug-user-data 2>&1 | head -10\r"
expect "root@"

puts ""
puts "🔍 DEBUG CONCLUÍDO - Analisando logs para identificar erro 400"

send "exit\r"
expect eof