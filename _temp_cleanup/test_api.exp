#!/usr/bin/expect -f

set timeout 20

spawn ssh root@31.97.28.231

expect {
    "Enter passphrase for key" {
        send "\r"
        exp_continue
    }
    "password:" {
        send "62uDLW4RJ9ae28EPVfp5yzT##\r"
    }
    timeout {
        puts "Connection timeout"
        exit 1
    }
}

expect "root@"

send "cd /root/eon\r"
expect "root@"

# Verificar status do PM2
send "pm2 status\r"
expect "root@"

# Testar API com mais detalhes
send "echo 'API Response:'\r"
expect "root@"

send "curl -X GET http://localhost:3000/api/bots -H 'Content-Type: application/json' 2>/dev/null\r"
expect "root@"

# Verificar bots no banco novamente
send "echo 'Bots no banco:'\r"
expect "root@"

send "sudo -u postgres psql -d eon_platform -c 'SELECT id, name, description FROM bots ORDER BY id;'\r"
expect "root@"

send "exit\r"
expect eof