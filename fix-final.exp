#!/usr/bin/expect -f

set timeout 30

spawn ssh -o StrictHostKeyChecking=no -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect "*assword*"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"
expect "*#*"

send "cd /root/eon\r"
expect "*#*"

puts "🔧 CORRIGINDO LINHA 103 COMPLETAMENTE..."

# Substituir linha inteira pela versão correta
send "sed -i '103c\\      const botsData = Array.isArray(response.data.bots) ? response.data.bots : (Array.isArray(response.data) ? response.data : []);' ./client/src/pages/OperationsPage.tsx\r"
expect "*#*"

# Verificar
send "sed -n '103p' ./client/src/pages/OperationsPage.tsx\r"
expect "*#*"

puts "🏗️ BUILD FINAL..."
send "cd client && npm run build\r"
expect {
    "*build folder is ready*" {
        puts "✅ BUILD SUCESSO FINALMENTE!"
    }
    "*compiled successfully*" {
        puts "✅ BUILD SUCESSO FINALMENTE!"
    }
    "*Failed*" {
        puts "❌ AINDA ERRO"
    }
    timeout {
        puts "⏰ Building..."
    }
}
expect "*#*"

send "cd /root/eon && pm2 restart all\r"
expect "*#*"

puts ""
puts "🎉 PRONTO! AGORA TESTE!"

interact