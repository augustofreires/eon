#!/usr/bin/expect -f

set timeout 30

spawn ssh root@31.97.28.231

expect {
    "Enter passphrase for key" {
        send "\r"
        exp_continue
    }
    "password:" {
        send "62uDLW4RJ9ae28EPVfp5yzT##\r"
        exp_continue
    }
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    "# " {
        puts "🔍 Checking current nginx configuration..."
        send "nginx -T | grep -A 20 'server_name iaeon.site'\r"
        expect "# "

        puts "🔧 Checking sites-enabled..."
        send "ls -la /etc/nginx/sites-enabled/\r"
        expect "# "

        puts "🔧 Reloading nginx with correct config..."
        send "nginx -s reload\r"
        expect "# "

        puts "🔧 Testing after reload..."
        send "curl -I https://iaeon.site/static/js/main.28d2975c.js\r"
        expect "# "

        send "exit\r"
    }
}

puts "✅ Nginx path fix completed"