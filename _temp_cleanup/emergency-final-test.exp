#!/usr/bin/expect -f

set timeout 10

spawn ssh -o PasswordAuthentication=yes -o PreferredAuthentications=password root@31.97.28.231

expect "password:"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"
expect "# "

# Get actual users from database
send "sudo -u postgres psql -d eon_platform -c \"SELECT id, name, email FROM users;\" -q -t\r"
expect "# "

# Test login with a real email from database
send "curl -X POST http://localhost:3001/api/auth/login -H 'Content-Type: application/json' -d '{\"email\":\"augustofreire.com@gmail.com\",\"password\":\"123456\"}'\r"
expect "# "

send "exit\r"
expect eof