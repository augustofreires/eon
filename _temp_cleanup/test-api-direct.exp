#!/usr/bin/expect -f

set timeout 30

spawn ssh -o StrictHostKeyChecking=no -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.97.28.231

expect "*assword*"
send "62uDLW4RJ9ae28EPVfp5yzT##\r"
expect "*#*"

send "cd /root/eon\r"
expect "*#*"

puts "🔍 VERIFICANDO ARQUIVO DE ROTAS DOS BOTS..."

send "cat server/routes/bots.js\r"
expect "*#*"

puts "🧪 TESTANDO API /api/bots DIRETAMENTE..."

# Testar endpoint sem autenticação primeiro
send "curl -X GET http://localhost:5000/api/bots -v\r"
expect "*#*"

puts "🔍 FAZENDO LOGIN E TESTANDO COM TOKEN..."

# Fazer login e pegar token
send "curl -X POST http://localhost:5000/api/auth/login -H 'Content-Type: application/json' -d '{\"email\":\"cliente@iaeon.com\",\"password\":\"123456\",\"isAdmin\":false}' -s\r"
expect "*#*"

puts "🔍 VERIFICANDO index.js PARA VER COMO ESTÁ CONFIGURADO..."

send "grep -A 10 -B 10 '/api/bots' server/index.js\r"
expect "*#*"

puts "🔍 VERIFICANDO SE MIDDLEWARE AUTH ESTÁ BLOQUEANDO..."

send "grep -A 5 -B 5 'authenticateToken' server/routes/bots.js\r"
expect "*#*"

interact